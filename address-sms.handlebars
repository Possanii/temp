<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Address Integration Test</title>
    <style>
      body { font-family: Arial, sans-serif; }
      .address-container { max-width: 480px; margin: 40px auto; padding: 24px; border: 1px solid #eee; border-radius: 8px; background: #fafafa; }
      h2 { color: #F45E43; margin-top: 0; }
      .label { font-weight: bold; }
      .required { color: #777; font-size: 12px; margin-top: 18px; }
    </style>
  </head>
  <body>
    <div class="address-container">
      <h2>Address Integration Test</h2>
      <hr>
      <div><span class="label">Post Office Box:</span> {{postOfficeBox}}</div>
      <div><span class="label">Extended Address:</span> {{extendedAddress}}</div>
      <div><span class="label">Street Address:</span> {{streetAddress}}</div>
      <div><span class="label">Locality (City):</span> {{locality}}</div>
      <div><span class="label">Region (State/Province):</span> {{region}}</div>
      <div><span class="label">Postal Code:</span> {{postalCode}}</div>
      <div><span class="label">Country Name:</span> {{countryName}}</div>
      <div class="required">
        <em>Required fields: locality, region, countryName</em>
      </div>
    </div>
  </body>
</html>
